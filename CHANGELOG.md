# Changelog

All notable changes to this project will be documented in this file.

## [unreleased]

### 🚀 Features

- *(foundation)* Phase 0 workspace, client, auth, ids, xtask, contracts
- *(docs)* Phase 1 doc extraction (xtask + per-op extracts + inventory)
- *(docs)* Extract Definitions section into per-type markdown
- *(root)* Implement get_system_status
- *(countries)* Implement list_countries, get_country
- *(tick_sizes)* Implement list_tick_sizes, get_tick_size
- *(markets)* Implement list_markets, get_market
- *(main_search)* Implement search
- *(news)* Implement get_news_item, list_news_sources
- *(login)* Implement start_login, verify_login, refresh_session, logout
- *(tradables)* Implement get_tradable_info, list_tradable_trades, get_suitability
- *(instruments)* Implement 9 instrument ops
- *(client)* Add post_form/put_form for FormData endpoints
- *(accounts)* Implement 6 account ops
- *(instrument_search)* Implement 6 search ops
- *(orders)* Implement 5 order ops with form-urlencoded body
- *(cli)* Amend OrdersCmd::run to thread fields slice (Phase 4 prep)
- *(cli)* Phase 4 wave 1 — root, countries, tick-sizes
- *(cli)* Phase 4 wave 2 — markets, news, login
- *(cli)* Replace 'nordnet login' with 'nordnet auth' + on-disk session
- *(cli)* Phase 4 wave 3 — accounts, tradables, search
- *(cli)* Phase 4 wave 4 — instruments, instrument-search, orders
- *(feed)* Scaffold nordnet-feed crate
- *(feed)* Newline-JSON codec + command serialization
- *(feed)* Public + private event payload types
- *(feed)* Public + private client types

### 🐛 Bug Fixes

- *(decimal)* Use arbitrary_precision serde for Decimal fields
- *(client)* Add put_empty for body-less PUT (login refresh_session)
- *(client)* [**breaking**] Remove auto-retry on 429/503
- *(cli)* [**breaking**] Atomic 0600 session save closes secret-leak race
- *(auth,wire,models)* [**breaking**] Align with live Nordnet API per research
- *(feed)* [**breaking**] Harden codec, transport, and event dispatcher per review
- *(feed)* [**breaking**] Production hardening per second review pass

### 💼 Other

- Resolve conflicts with ccairgap/misty-penguin-7006

### 🚜 Refactor

- *(models)* Phase 3X cross-endpoint type consistency
- *(models)* [**breaking**] Complete tradables OrderType→AllowedOrderType rename
- *(workspace)* [**breaking**] Extract nordnet-model from nordnet-api

### 📚 Documentation

- Add build process spec and saved Nordnet API v2 reference HTML
- Relax network rule to Nordnet-only
- *(process)* Drop phase 2/2c; orders 5 ops; correct counts
- *(process)* Record auth deviation, fixture provenance, orders-cli gate, pipeline state
- *(process)* Update state log after wave 1 + decimal fix
- *(process)* Record wave 4 done; foundation amendments logged
- *(process)* Mark Phase 3X done in state log
- *(process)* Mark Phase 4 done with final wave commit hash
- *(process)* Close Phase 5; drop per-op CLI wiremock smoke
- *(auth)* Record refresh-without-expires_in assumption
- Write README, add licenses + repo metadata
- *(api)* Add resource group index + HTTP path doc aliases
- *(feed)* Add lifecycle, mapping tables, protocol URLs, and aliases
- *(cli)* Lift help, README, completions cmd
- *(model)* Field docs, # Errors sections, fix doc warnings
- *(security)* Document current CLI threat model + preserve hardened-mode spec

### 🧪 Testing

- *(login)* Align fixtures with nordnet examples repo
- *(feed)* Codec, command, event, client coverage

### ⚙️ Miscellaneous Tasks

- *(feed)* Regenerate mod files + finalize crate
- Prep repo for open source release
- Add crates.io publish scaffolding + per-crate docs
- Commit deprecated spec

<!-- generated by git-cliff -->
